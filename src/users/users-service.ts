import {BlogInputModel} from "../types/blog-types";
import {UserInputModel} from "../types/users-types";
import {ObjectId} from "mongodb";

export const usersService = {
    createUser (body: UserInputModel) {
        const newUser = {


        }
    }
}
